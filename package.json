{
  "private": true,
  "engines": {
    "npm": ">=11.8.0"
  },
  "dependencies": {
    "@ministryofjustice/frontend": "^9.0.0",
    "govuk-frontend": "^6.1.0",
    "hmrc-frontend": "^7.14.0",
    "moment": "^2.30.1",
    "sass": "^1.99.0"
  },
  "scripts": {
    "build:import": "sass --silence-deprecation=import --pkg-importer=node import.scss",
    "build:use": "sass --silence-deprecation=import --pkg-importer=node use.scss",
    "test": "jest",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "diff": "^9.0.0",
    "jest": "^30.4.2",
    "patch-package": "^8.0.1"
  }
}
